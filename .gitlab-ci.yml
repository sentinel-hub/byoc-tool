image: sentinelhub/gdal-jdk:gdal3-jdk8

stages:
  - build
  - test
  - publish

build:
  stage: build
  script:
    - ./gradlew shadowJar
  artifacts:
    paths:
      - build/libs/*.jar
    expire_in: 1 week
  only:
    - master

test:
  stage: test
  script:
    - ./gradlew test

publish:
  stage: publish
  script:
    - ./gradlew --info publish
